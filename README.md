# 等会儿吃什么

一个手机优先的网页小应用：输入地址后，查询附近 500 米内的餐厅，再随机抽一家去吃。

## 本地使用

直接打开 `index.html` 即可体验界面。默认没有地图 Key，会使用演示餐厅数据。

## Vercel 部署

真实餐厅数据通过 Vercel Serverless Function 代理请求高德开放平台，避免把 Key 暴露在前端代码里。

需要在 Vercel 项目环境变量里配置：

- `AMAP_KEY`：高德 Web 服务 API Key。

配置完成后，搜索地址会通过 `/api/amap` 查询该地址方圆 500 米内的餐饮服务。
